{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for Soufmer's main window.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:event:default",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir"
  ]
}
